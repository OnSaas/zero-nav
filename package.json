{
  "name": "zero-nav",
  "version": "3.0.0",
  "private": true,
  "description": "Cloudflare Worker 导航站 - Hono + KV",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "npx tsx scripts/migrate-from-zero-nav.ts"
  },
  "dependencies": {
    "hono": "^4.7.11",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20251127.0",
    "tsx": "^4.19.4",
    "typescript": "^5.7.2",
    "wrangler": "^3.114.0"
  }
}
